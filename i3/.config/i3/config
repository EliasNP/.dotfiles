# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:JetBrains Mono Medium 9

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:JetBrains Mono Medium 9

focus_follows_mouse no

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Cambio de fondo de escritorio
#exec --no-startup-id feh --bg-scale ~/Documentos/PERSONAL\ DATA/WALLPAPER/photo_2018-11-18_14-22-22.jpg
exec --no-startup-id feh --randomize --bg-scale ~/Documentos/PERSONAL\ DATA/WALLPAPER/*
exec --no-startup-id compton --config ~/.config/compton.conf

# start a terminal
#bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+t exec gnome-terminal

# start File Manager "Nemo"
bindsym $mod+f exec nemo 

bindsym $mod+Control+l exec ~/scripts/lock.sh

# Screen Shots
bindsym Print exec scrot '%Y-%m-%d_$wx$h_scrot.png' -e 'mv $f ~/Imágenes/shoot'
bindsym Control+Print exec scrot -u '%Y-%m-%d_$wx$h_scrot.png' -e 'mv $f ~/Imágenes/shoot'
bindsym Shift+Print exec scrot -s '%Y-%m-%d_$wx$h_scrot.png' -e 'mv $f ~/Imágenes/shoot'
bindsym $mod+Print exec gnome-screenshot -a

# kill focused window
bindsym $mod+Shift+q kill

# start ulauncher (a program launcher)
bindsym $mod+d exec ulauncher 

# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+ntilde focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+ntilde move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+Control+F11 fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Workspace naming
set $workspace1 "1: E-Mail" 
set $workspace2 "2: Terminal"
set $workspace3 "3: Navegador"
set $workspace4 "4: Archivos"
set $workspace5 "5: Editor Codigo"
set $workspace6 "6: WinBox"
set $workspace7 "7: Libre Calc"
set $workspace8 "8: Otros- Varios"
set $workspace9 "9: Mensajeria"
set $workspace10 "10: Musica"
set $workspace11 "11: Extra"
set $workspace12 "12: Extra-2"

# use workspaces on different monitors
workspace $workspace1 output eDP-1
workspace $workspace2 output HDMI-1
workspace $workspace3 output HDMI-1
workspace $workspace4 output HDMI-1
workspace $workspace5 output HDMI-1
workspace $workspace6 output HDMI-1
workspace $workspace7 output HDMI-1
workspace $workspace8 output HDMI-1
workspace $workspace9 output eDP-1
workspace $workspace10 output eDP-1
workspace $workspace11 output HDMI-1
workspace $workspace12 output eDP-1

# switch to workspace
bindsym $mod+F1 workspace $workspace1
bindsym $mod+F2 workspace $workspace2
bindsym $mod+F3 workspace $workspace3 
bindsym $mod+F4 workspace $workspace4 
bindsym $mod+F5 workspace $workspace5
bindsym $mod+F6 workspace $workspace6 
bindsym $mod+F7 workspace $workspace7 
bindsym $mod+F8 workspace $workspace8 
bindsym $mod+F9 workspace $workspace9
bindsym $mod+F10 workspace $workspace10
bindsym $mod+F11 workspace $workspace11 
bindsym $mod+F12 workspace $workspace12 

# add HDMI monitor when connected
exec --no-startup-id xrandr --output HDMI-1 --left-of eDP-1
bindsym $mod+m exec --no-startup-id xrandr --output HDMI-1 --auto --left-of eDP-1

# move focused container to workspace
bindsym $mod+Shift+F1 move container to workspace $workspace1
bindsym $mod+Shift+F2 move container to workspace $workspace2
bindsym $mod+Shift+F3 move container to workspace $workspace3
bindsym $mod+Shift+F4 move container to workspace $workspace4
bindsym $mod+Shift+F5 move container to workspace $workspace5
bindsym $mod+Shift+F6 move container to workspace $workspace6
bindsym $mod+Shift+F7 move container to workspace $workspace7
bindsym $mod+Shift+F8 move container to workspace $workspace8
bindsym $mod+Shift+F9 move container to workspace $workspace9
bindsym $mod+Shift+F10 move container to workspace $workspace10
bindsym $mod+Shift+F11 move container to workspace $workspace11
bindsym $mod+Shift+F12 move container to workspace $workspace12

# Forcing apps on workspaces
# Terminal Apps
assign [class="Terminus"] $workspace2
assign [class="gnome-terminal"] $workspace2
# Mail apps
assign [class="Thunderbird"] $workspace1
# Web apps
assign [class="Firefox"] $workspace3
assign [class="Google-chrome"] $workspace3
# Coding apps
assign [class="Atom"] $workspace5
assign [class="Code"] $workspace5
assign [class="Sublime"] $workspace5
# File apps
assign [class="Nemo"] $workspace4
# apps
assign [class="winbox"] $workspace6
assign [class="wine"] $workspace6
# Texting apps
assign [class="TelegramDesktop"] $workspace9
assign [class="Twinkle"] $workspace9
assign [class="Zoiper"] $workspace9
# Music apps
#assign [class="Spotify"] $workspace10
for_window [class="Spotify"] move to workspace $workspace10
assign [class="Headset"] $workspace10

# reload the configuration file
bindsym $mod+Shift+c reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'Has pulsado las teclas para salir de i3. ¿De verdad quieres salir de i3? Esto cerrará tu sesión.' -b 'Sí, salir de i3' 'i3-msg exit'"

bindsym $mod+Ctrl+greater move workspace to output right
bindsym $mod+Ctrl+less move workspace to output left

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym ntilde resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Color config
set $bg-color 	         #000000
set $inactive-bg-color   #000000
set $text-color          #5DA8F4
set $inactive-text-color #D8D8D8
set $urgent-bg-color     #E53935
set $blue-color          #5DA8F4

# Window colors
client.focused          $bg-color           $bg-color          $text-color          #00ff00
client.unfocused        $inactive-bg-color $inactive-bg-color $inactive-text-color #00ff00
client.focused_inactive $inactive-bg-color $inactive-bg-color $inactive-text-color #00ff00
client.urgent           $blue-color    $blue-color   $text-color          #5DA8F4



#############################
### settings for i3-gaps: ###
#############################

# Set inner/outer gaps
gaps inner 15
gaps outer 5

# Activate smart borders (always)
smart_borders on


#============================================================================
#                               i3wm bar                                    #
#============================================================================

bindsym $mod+b bar mode toggle
bar {
	# Bar program
        status_command i3blocks -c ~/.i3/i3blocks.conf
        #status_command i3status

        # Position of the bar
        position top
        
        # Bar colors
        colors {
		background $bg-color
		separator #5DA8F4
		#                  border             background         text
		focused_workspace  $bg-color          $bg-color          $text-color
		inactive_workspace $inactive-bg-color $inactive-bg-color $inactive-text-color
	        urgent_workspace   $blue-color   $blue-color   $text-color
        }
}

# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +5% #increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -5% #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound

# Increase brightness
#bindsym XF86MonBrightnessUp exec “xbacklight -inc 5”

# Decrease brightness
#bindsym XF86MonBrighnessDown exec “xbacklight -dec 5”

# Touchpad controls
#bindsym XF86TouchpadToggle exec /home/enieva/bin/toggletouchpad # toggle touchpad

# Media player controls
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioPause exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

# Personal KeyBindings
#bindsym $mod+x exec i3lock --color "$bg-color"
#xautolock

# exec --no-startup-id xautolock -time 5 -locker ~/.i3/matrixlock/matrixlock.py
# exec --no-startup-id xautolock -time 5 -locker "~/scripts/lock.sh"
bindsym $mod+x exec ~/scripts/lock.sh
# bindsym $mod+x exec ~/.i3/matrixlock/matrixlock.py

bindsym $mod+Shift+f exec firefox
bindsym $mod+Shift+x exec shutdown -h now
bindsym $mod+Shift+v exec code
bindsym $mod+Shift+b exec blueman-manager

# Remove green border on the left
for_window [class="^.*"] border pixel 0

# Apps on start

# bluetooth
exec --no-startup-id blueberry-tray

# network
exec --no-startup-id nm-applet

# num lock activated
exec_always --no-startup-id numlockx on

# nitrogen
exec --no-startup-id "nitrogen --restore"

exec_always compton
exec kdeconnect-indicator
exec thunderbird
exec nemo
exec firefox
exec $HOME/Descargas/Telegram/Telegram
exec headset
exec subl
exec gnome-terminal
exec winbox



